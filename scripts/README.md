# Scripts

## Validate levels

Run:

```bash
node scripts/validate-levels.mjs
```

Checks that every SQL solution in `data/levels.json` can be executed by the current parser/engine without returning an error row.
